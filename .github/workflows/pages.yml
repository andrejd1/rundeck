# Publishes the landing page (site/) to GitHub Pages.
# Needs Settings -> Pages -> Source: "GitHub Actions". GitHub serves Pages
# for free from public repositories only; a private repo needs a paid plan.
name: Landing page

on:
  push:
    branches: [main]
    paths: ["site/**", "docs/screenshots/**", "shared/fields.js", "shared/license.js"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node site/build.mjs
      - run: mkdir -p _site && cp site/index.html _site/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
